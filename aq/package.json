{
  "name": "aq",
  "version": "0.0.2",
  "description": "Aquin CLI. The current directory is the train.",
  "type": "module",
  "bin": {
    "aq": "./bin/aq"
  },
  "files": [
    "bin",
    "dist",
    "kernel",
    "assets"
  ],
  "scripts": {
    "build": "tsc && mkdir -p dist/job && cp src/job/job-wait.mjs dist/job/job-wait.mjs",
    "prepare": "npm run build",
    "aq": "node dist/cli.js"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
