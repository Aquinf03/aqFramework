# Real backends for aq kernel. Install into the same Python that runs aq train.
#
#   pip install -r aq/kernel/requirements.txt
#
# QLoRA (4-bit) needs CUDA + bitsandbytes. On Mac/CPU use LoRA without bits.

tokenizers>=0.19.0
torch>=2.1.0
transformers>=4.40.0
peft>=0.11.0
accelerate>=0.30.0
datasets>=2.19.0
scikit-learn>=1.4.0
joblib>=1.3.0
xgboost>=2.0.0
lightgbm>=4.0.0
catboost>=1.2
# optional:
# bitsandbytes>=0.43.0   # CUDA only, for QLoRA
