# v0.0.3 — to be shipped

## New

- **`speculative: true`** — real assisted decode at serve. Set a smaller **`draft_model:`** (same tokenizer family). Train records it; `aq serve` uses Hugging Face `assistant_model`.

## Fixes
